name: checks

on:
  push:
  pull_request:

jobs:
  windows-checks:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "0.10.10"
      # Installing the locked dependencies is the only step that uses the network.
      - run: uv sync --locked
      - run: uv run ruff format --check .
      - run: uv run ruff check .
      - run: uv run mypy src lab
      # The lab containers are not started in CI, so their integration tests are
      # deselected here and run by the owner on the reference machine.
      - run: uv run pytest -m "not docker"
